---
/**
 * 天气小组件
 * 显示访客当前位置的天气信息
 */
import WidgetLayout from './WidgetLayout.astro';
---

<WidgetLayout name="天气" id="weather-widget-wrapper">
<div id="weather-widget" class="weather-widget">
	<div class="weather-loading">
		<span class="loading-icon">🌤️</span>
		<span class="loading-text">获取天气中...</span>
	</div>
	<div class="weather-content hidden">
		<div class="weather-main">
			<span id="weather-icon" class="weather-icon">☀️</span>
			<span id="weather-temp" class="weather-temp">--°C</span>
		</div>
		<div class="weather-info">
			<span id="weather-desc" class="weather-desc">--</span>
			<span id="weather-location" class="weather-location">📍 --</span>
		</div>
		<div class="weather-details">
			<span id="weather-humidity" class="detail-item">💧 --%</span>
			<span id="weather-wind" class="detail-item">🌬️ --</span>
		</div>
	</div>
	<div class="weather-error hidden">
		<span>😅 获取天气失败</span>
	</div>
</div>
</WidgetLayout>

<style>
	.weather-widget {
		padding: 8px 0;
	}

	.weather-loading {
		display: flex;
		align-items: center;
		gap: 8px;
		justify-content: center;
		padding: 8px 0;
	}

	.loading-icon {
		font-size: 1.5rem;
		animation: pulse 1.5s infinite;
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.5; }
	}

	.loading-text {
		color: var(--text-muted);
		font-size: 0.875rem;
	}

	.weather-content {
		display: flex;
		flex-direction: column;
		gap: 8px;
	}

	.weather-content.hidden,
	.weather-error.hidden {
		display: none;
	}

	.weather-main {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 12px;
	}

	.weather-icon {
		font-size: 2.5rem;
	}

	.weather-temp {
		font-size: 2rem;
		font-weight: 700;
		color: var(--text-color);
	}

	.weather-info {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 4px;
	}

	.weather-desc {
		font-size: 1rem;
		color: var(--text-color);
	}

	.weather-location {
		font-size: 0.75rem;
		color: var(--text-muted);
	}

	.weather-details {
		display: flex;
		justify-content: center;
		gap: 16px;
		margin-top: 4px;
		padding-top: 8px;
		border-top: 1px dashed var(--line-divider);
	}

	.detail-item {
		font-size: 0.75rem;
		color: var(--text-muted);
	}

	.weather-error {
		text-align: center;
		color: var(--text-muted);
		font-size: 0.875rem;
		padding: 8px 0;
	}
</style>

<script>
	import { getVisitorLocation } from '../../utils/supabase';

	// 天气图标映射
	const weatherIcons: Record<string, string> = {
		'Clear': '☀️',
		'Sunny': '☀️',
		'晴': '☀️',
		'Partly cloudy': '⛅',
		'多云': '⛅',
		'Cloudy': '☁️',
		'阴': '☁️',
		'Overcast': '☁️',
		'Mist': '🌫️',
		'雾': '🌫️',
		'Fog': '🌫️',
		'Light rain': '🌦️',
		'小雨': '🌦️',
		'Rain': '🌧️',
		'中雨': '🌧️',
		'Heavy rain': '🌧️',
		'大雨': '🌧️',
		'暴雨': '🌧️',
		'Thunderstorm': '⛈️',
		'雷阵雨': '⛈️',
		'Snow': '🌨️',
		'雪': '🌨️',
		'Light snow': '🌨️',
		'小雪': '🌨️',
		'Heavy snow': '❄️',
		'大雪': '❄️',
		'暴雪': '❄️',
		'Sleet': '🌨️',
		'雨夹雪': '🌨️',
		'default': '🌤️'
	};

	function getWeatherIcon(condition: string): string {
		for (const [key, icon] of Object.entries(weatherIcons)) {
			if (condition.toLowerCase().includes(key.toLowerCase())) {
				return icon;
			}
		}
		return weatherIcons['default'];
	}

	// 检测是否为国内用户
	function isChina(country: string): boolean {
		const chinaNames = ['china', '中国', '中华人民共和国', 'cn', 'chn'];
		return chinaNames.some(name => country.toLowerCase().includes(name.toLowerCase()));
	}

	// 使用和风天气 API（国内）- 免费版
	async function getQWeather(city: string): Promise<any> {
		// 和风天气免费 API（无需 Key 的公共接口）
		// 使用 wttr.in 的中文版本作为替代
		const response = await fetch(
			`https://wttr.in/${encodeURIComponent(city)}?format=j1&lang=zh`,
			{ signal: AbortSignal.timeout(8000) }
		);
		if (!response.ok) throw new Error('Weather API failed');
		return response.json();
	}

	// 使用 wttr.in（国外）
	async function getWttrWeather(city: string): Promise<any> {
		const response = await fetch(
			`https://wttr.in/${encodeURIComponent(city)}?format=j1`,
			{ signal: AbortSignal.timeout(8000) }
		);
		if (!response.ok) throw new Error('Weather API failed');
		return response.json();
	}

	async function initWeather() {
		const widget = document.getElementById('weather-widget');
		if (!widget) return;

		const loading = widget.querySelector('.weather-loading');
		const content = widget.querySelector('.weather-content');
		const error = widget.querySelector('.weather-error');

		try {
			// 使用与访客地图相同的定位方法
			const location = await getVisitorLocation();
			if (!location) throw new Error('Location failed');

			console.log('🌤️ 天气定位:', location);

			// 根据国家选择天气 API
			const isChinaUser = isChina(location.country);
			const cityName = location.city || location.province;
			
			let weather;
			if (isChinaUser) {
				console.log('🌤️ 使用国内天气源');
				weather = await getQWeather(cityName);
			} else {
				console.log('🌤️ 使用国际天气源');
				weather = await getWttrWeather(cityName);
			}

			// 解析天气数据
			const current = weather.current_condition[0];
			const temp = current.temp_C;
			const desc = current.lang_zh?.[0]?.value || current.weatherDesc[0].value;
			const humidity = current.humidity;
			const windSpeed = current.windspeedKmph;

			// 更新 UI
			const iconEl = document.getElementById('weather-icon');
			const tempEl = document.getElementById('weather-temp');
			const descEl = document.getElementById('weather-desc');
			const locEl = document.getElementById('weather-location');
			const humidityEl = document.getElementById('weather-humidity');
			const windEl = document.getElementById('weather-wind');

			if (iconEl) iconEl.textContent = getWeatherIcon(desc);
			if (tempEl) tempEl.textContent = `${temp}°C`;
			if (descEl) descEl.textContent = desc;
			if (locEl) locEl.textContent = `📍 ${location.city || location.province}${location.province && location.city ? ', ' + location.province : ''}`;
			if (humidityEl) humidityEl.textContent = `💧 ${humidity}%`;
			if (windEl) windEl.textContent = `🌬️ ${windSpeed}km/h`;

			// 显示内容
			loading?.classList.add('hidden');
			content?.classList.remove('hidden');

		} catch (err) {
			console.warn('天气获取失败:', err);
			loading?.classList.add('hidden');
			error?.classList.remove('hidden');
		}
	}

	// 延迟加载天气，等待定位完成
	setTimeout(initWeather, 2000);

	// Swup 支持
	document.addEventListener('astro:page-load', () => {
		setTimeout(initWeather, 2000);
	});
</script>